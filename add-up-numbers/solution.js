/**
 * @param {string} str - A string containing numbers separated by commas.
 * @returns {number} The sum of the numbers in the string.
 *
 */

const addUpNumbers = (num) => {
	return (num * (num + 1)) / 2;
};

export default addUpNumbers;